#include "main.h"

/**
 *-isdigit.c - Check if a character is a digit
 *@x and y the two numbers to be multiplied
 * Returns 1 if c is a digit
 *Returns 0 otherwise
*/

int mul(int x, int y)

{
	return (x * y);
}
